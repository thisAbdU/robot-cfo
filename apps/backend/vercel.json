{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "cd ../.. && corepack enable pnpm && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter backend exec prisma generate && pnpm --filter backend build",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 60,
      "includeFiles": "dist/src/**"
    }
  }
}
